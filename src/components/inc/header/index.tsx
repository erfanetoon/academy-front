import MobileMenu from '../mobileMenu';
import Sidebar from '../sidebar';

const Header = () => {
    return (
        <header>
            <div></div> Header
            <Sidebar />
            <MobileMenu />
        </header>
    );
};

export default Header;
